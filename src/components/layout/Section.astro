---
import type { SectionProps } from '@/types';

const { id, title, class: className } = Astro.props as SectionProps;
---

<section id={id} class={className}>
	<header class="major">
		<h2>{title}</h2>
		<slot name="description" />
	</header>
	<slot />
</section>
